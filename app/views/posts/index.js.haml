$('.posts').append('#{j render @posts}');
- if @posts.next_page
  $('.pagination').replaceWith('#{j paginate @posts}');
- else
  $(window).off('scroll');
  $('.pagination').remove();
  $('.posts').append('<span> Post terminati</span>');
